<h1 class="text-center">Competitions!</h1>
<br><br>

<table class="table">
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Competitors</th>
    <th colspan="3"></th>
  </tr>

  <% @competitions.each do |competition| %>
    <tr>
      <td><%= competition.title %></td>
      <td><%= competition.description %></td>
      <td><%= competition.users.count %></td>
      <td><%= link_to 'show', competition_path(competition)%></td>
      <td><%= link_to 'edit', edit_competition_path(competition) %></td>
      <td><%= link_to 'delete', competition_path(competition),
              method: :delete,
              data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <%end %>
</table>


<div class="btn btn-default">
<%= link_to 'New Competion', new_competition_path %>
</div>